package com.boot2.karat.matchresults;

/**
 * Administrator evaluation decision for a match (or for a team's season snapshot).
 * Karat-style: approve (accept) vs reject (deny) based on points / W-D-L rules.
 */
public enum AdminDecision {
    APPROVED,
    REJECTED
}
