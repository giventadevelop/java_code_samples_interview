/**
 * Karat / Indeed interview practice: aggregate visit counts across domain and all parent subdomains.
 *
 * <p>See {@link com.boot2.karat.subdomaincounts.SubdomainCountDemo} and {@code README.md}.
 * Key Java tip: {@code domain.split("\\.")} — never {@code split(".")}.
 */
package com.boot2.karat.subdomaincounts;
